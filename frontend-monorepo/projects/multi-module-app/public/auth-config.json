{
  "mePath": "/api/me",
  "sessionPath": "/api/auth/session",
  "loginPath": "/api/auth/login",
  "logoutPath": "/api/auth/logout",
  "defaultReturnUrl": "/",
  "notes": "Frontend auth is handled by the backend BFF session. Update these paths if your backend uses different auth endpoints."
}
